# 概要

## 1. 目的

本アプリケーションは、
画像と位置情報を組み合わせた匿名型投稿サービスである。

ユーザーは画像とコメントを投稿でき、 画像に含まれる位置情報をもとに投稿は地図上へ可視化される。
他のユーザーはログイン後に投稿を閲覧し、返信できる。

本プロジェクトでは、 
要件定義から設計、実装、テスト、デプロイ、公開までの一連のプロセスを通して、
Webサービスをエンドツーエンドで構築することを目的とする。

また、以下の技術的課題に取り組む。

- MPAとSPAを組み合わせたハイブリッド構成の実装
- 画像アップロードおよびEXIF情報の解析
- 地図APIとの連携
- 認証・認可設計
- ログ設計およびエラーハンドリング
- クラウド環境へのデプロイおよび公開運用

拡張性・保守性・運用性を考慮した設計を行うことを目的とする。

## 2. スコープ

本プロジェクトでは、最小構成でサービスとして動作する
MVP（Minimum Viable Product）の実装を対象とする。

### 2.1 提供機能（In scope）

以下の機能を実装対象とする。

- ユーザー登録
- ログイン
- 投稿作成（画像 + コメント）
- 投稿への返信
- 投稿タイムライン表示
- 投稿検索
- 投稿の地図表示（位置情報連動）
- マイページ（自分の投稿一覧・返信一覧）

投稿・閲覧・返信という基本的なSNS体験が成立する状態を目標とする。

### 2.2 今回対象外とする機能（Out of scope）

以下の機能は、初期リリースでは実装対象外とする。

- いいね機能
- パスワード再設定
- リアルタイム通知
- 投稿削除機能
- アカウント削除機能
- メールアドレス認証

上記は拡張機能として将来的に追加可能な設計とする。

## 3. 想定ユーザー

本アプリケーションは、以下のようなユーザーを想定する。

- 外出機会が多く、出先で写真を撮る習慣のある人物
- ニックネームやプロフィールによって識別されるSNSに抵抗を感じる人物
- 既存の写真共有SNSとは異なる、匿名性の高い環境で投稿を行いたい人物

### 利用端末
- スマートフォン（主要利用端末）
- PC

本サービスはレスポンシブデザインにより、単一のWebアプリケーションとして両端末に対応する。

### 想定利用規模

本サービスは初期段階において、 登録ユーザー数 〜100人規模、 同時接続数 最大10人程度を想定する。

将来的な拡張を見据えた設計とするが、 初期フェーズでは小規模運用を前提とする。

## 4. 主要ユースケース

### UC-01：投稿を作成する

ユーザーは画像とコメントをアップロードし、投稿を作成する。
画像に位置情報が含まれている場合、 投稿は自動的に地図上へマッピングされる。

---

### UC-02：投稿を閲覧する

ユーザーはタイムライン画面から他の投稿を閲覧できる。
投稿詳細画面では、画像・コメント・返信を確認できる。

---

### UC-03：投稿に返信する

ユーザーは他のユーザーの投稿に対して返信コメントを投稿できる。
返信はスレッド形式で表示される。

---

### UC-04：地図から投稿を探索する

ユーザーは地図画面を表示し、 位置情報に基づいて投稿を視覚的に探索できる。
マーカーを選択すると、対応する投稿詳細を表示する。

---

### UC-05：自分の投稿履歴を確認する

ユーザーはマイページから、 自身が投稿した内容および返信履歴を一覧で確認できる。

## 5. 画面一覧
- ログイン（ログイン / 新規会員登録）
- タイムライン（投稿一覧 / 投稿詳細 / 投稿作成 / 返信作成 / 返信への返信）
- 検索（検索結果一覧）
- 投稿マップ（地図に投稿を表示）
- マイページ（自分の投稿一覧 / 自分の返信一覧）
  ※詳細は `05_screen_spec.md` へ

## 6. データの特徴（ざっくり）
- 主要エンティティ：User / Post / Reply / Like / Image / Location など
- 位置情報：画像EXIF由来 or ユーザー入力（どちらか/両方）
- 公開識別子：API や外部参照では `public_id` / ULID を利用する
- 画像：保存方式（開発：ローカル、stg / prod：S3）
- 画像配信：stg / prod は CloudFront 導入まで短命な S3 presigned URL を暫定利用する

## 7. 非機能要件（最低限）
### 7.1 性能・規模（目安）

- 投稿件数：1日あたり 〜10件程度を想定する
- 画像サイズ：1投稿あたり 5MB以内を想定する

#### レスポンス目標
通常操作（テキスト中心のAPI）は、P95で1.0秒以内を目標とし、P95で2.0秒以内を許容範囲とする。
画像アップロード（最大5MB）はネットワーク状況に依存するため、P95で5〜10秒以内を目標とし、P95で15秒以内を許容範囲とする。

画面表示については、初回表示（MPA相当）を2秒以内、画面遷移（SPA相当）を1秒以内の体感を目標とする。

### 7.2 セキュリティ

#### 認証方式
認証は Spring Security によるセッション管理（Cookie）を採用する。
ログイン成功時にサーバー側でセッションを発行し、以後のリクエストはCookieにより認証状態を維持する。
- ログイン API は `POST /api/login`、ログアウト API は `POST /api/logout` を利用する
- Cookie属性：Secure / HttpOnly / SameSite を設定する
- CSRF対策：セッションCookie運用のため、CSRF対策を有効化する（Spring Securityの仕組みを利用）

#### 認可
本サービスでは、画面・API・画像配信を原則認証必須とする。

- ログイン画面、サインアップ導線、静的アセット、ヘルスチェックを除きログイン必須とする
- 投稿の閲覧、検索、地図表示、マイページ閲覧はログイン必須とする
- 投稿作成、返信作成はログイン必須とする
- 投稿の編集・削除（将来実装）は投稿者本人のみ可能とする
- 返信の編集・削除（将来実装）は返信者本人のみ可能とする

認可はサーバー側で強制し、クライアント側の表示制御のみを根拠にしない。

#### 入力バリデーション方針
入力値は「クライアント側とサーバー側の両方」で検証し、最終的な保証はサーバー側で行う。

- クライアント側：ユーザー体験向上のため、必須チェック・文字数制限などの基本検証を行う
- サーバー側：Bean Validation（Jakarta Validation）等を用いて、必須/形式/長さ/範囲を検証する
- 不正入力時：HTTP 400（Bad Request）を返却し、エラーレスポンス形式を統一する

また、XSS対策として、表示時はHTMLとして解釈されない形でレンダリングする（テンプレート/フロント側でエスケープ）。

#### アップロード制限（画像）
画像アップロードは悪用リスク（DoS、ストレージ逼迫、任意ファイル混入）を考慮し、以下の制限を設ける。

- サイズ上限：1ファイルあたり 5MB 以下
- 形式：JPEG / PNG のみ許可（必要に応じて拡張）
- MIME Type 検証：Content-Type のみを信用せず、サーバー側で実ファイルを判定する
- 拡張子検証：許可形式以外は拒否する（MIME判定と併用）
- 画像デコード検証：画像として読み取れないファイルは拒否する
- EXIF取扱い：位置情報の抽出に利用するが、EXIFをそのまま公開しない（必要な情報のみ保持する）
- リクエスト制限：アップロードAPIはレート制限/タイムアウトを設定する

### 7.3 可用性・バックアップ

本サービスは初期段階では小規模運用を前提とし、
高可用性（マルチAZ等）よりも「データ保全」と「復旧手順の明確化」を優先する。

#### 目標
- 重大障害時に、バックアップから復旧できること（データ消失の最小化）
- 復旧手順がドキュメント化されていること

#### バックアップ方針（暫定）
- DB：定期バックアップ（自動バックアップ/スナップショット相当）を取得し、一定期間保持する
- 画像：保存先に応じて、日次バックアップまたはバージョニング等により復旧可能性を確保する
- 設定値/Secrets：環境変数等で管理し、リポジトリには保存しない

#### 復旧方針（暫定）
- DB復旧：直近スナップショットから復元し、アプリケーションを再接続する
- 画像復旧：バックアップまたは保存先の復旧機能を利用して復元する

※具体的な構成（Single / Multi-AZ、保存先、保持期間、RPO/RTO）はデプロイ方式決定後に確定する。

### 7.4 ログ・監視

本サービスでは、障害解析・不正利用検知・運用状況把握を目的としてログを出力する。

#### ログ方針

ログは以下の種類に分類する。

1. アプリケーションログ
    - リクエスト処理の開始・終了
    - 主要処理の成功・失敗
    - 例外発生時のスタックトレース
    - 画像アップロード処理結果

2. 認証・認可ログ
    - ログイン成功 / 失敗
    - 認可エラー（403）
    - 不正アクセス試行

3. アクセスログ
    - HTTPメソッド
    - エンドポイント
    - レスポンスステータス
    - 処理時間

ログにはリクエストごとに一意のトレースIDを付与し、
障害解析時に関連ログを横断的に追跡できるようにする。

#### 個人情報の取り扱い

- パスワードはログに出力しない
- セッションIDはログに出力しない
- メールアドレス等の識別情報は必要に応じてマスキングする
- 画像ファイルの実データはログに出力しない

#### ログレベル

- INFO：通常操作ログ
- WARN：想定内エラー（バリデーション失敗など）
- ERROR：例外・障害発生時
- DEBUG：開発環境のみ有効

#### 監視方針

- アプリケーション異常終了の検知
- HTTP 500 エラーの増加監視
- レスポンスタイムの異常増加検知
- ディスク使用量の監視（画像保存領域）

具体的なログ出力先および監視基盤は、
デプロイ方式決定後に確定する。

## 8. 技術スタック

### Frontend
- 言語/ランタイム：Node.js（20.19.0 以上 / 22.12.0 以上）
- フレームワーク：Vue 3
- 状態管理：Pinia
- UI：Vuetify 3 / Material Design Icons（@mdi/font）
- HTTPクライアント：Axios
- 地図表示：Leaflet
- ビルド/開発環境：Vite
- Lint：ESLint / Oxlint

### Backend
- 言語：Java 17
- フレームワーク：Spring Boot 4
- Web：Spring Web MVC
- テンプレート：Thymeleaf（MPA用途）
- 認証/認可：Spring Security（セッションCookie）
- バリデーション：Spring Validation（Jakarta Validation）
- 永続化：Spring Data JPA / MyBatis（併用）
- DB（本番想定）：MySQL（mysql-connector-j）
- 開発/検証：H2 Console
- 監視：Spring Boot Actuator
- 画像メタデータ解析：metadata-extractor

### Testing
- JUnit Platform
- Spring Boot Test（Web MVC / Security / Thymeleaf / Validation / Actuator）
- MyBatis Starter Test

### 構成方針（補足）
- 画面はMPA（Thymeleaf）とSPA（Vue）を組み合わせたハイブリッド構成とする
- SPA領域ではVue Router / Pinia を用いて状態と画面遷移を管理する（必要箇所のみ）

## 9. 環境（Local / Dev / Prod）

### 9.1 Local（開発環境）

ローカル環境では以下の構成で開発を行う。

- Frontend：Vite 開発サーバー（ポート 5173）
- Backend：Spring Boot アプリケーション（ポート 8080）
- DB：H2 DB（開発用）
- 画像保存：ローカルファイルシステム

フロントエンドとバックエンドは別プロセスで起動し、API通信は localhost 経由で行う。
開発時はデバッグログ（DEBUGレベル）を有効化する。

---

### 9.2 Dev（検証環境）

Dev環境は本番構成に近い形で動作検証を行う環境とする。

- ビルド済みFrontendをBackendに統合、または静的配信環境へ配置
- DBは本番相当のエンジンを使用
- ログレベルはINFO以上とする

具体的な構成はデプロイ方式決定後に確定する。

---

### 9.3 Prod（本番環境）

本番環境はクラウド環境（AWSを想定）へデプロイし、
外部公開する。

- HTTPS通信を必須とする
- 本番用DBを使用
- 画像保存は永続ストレージを利用する
- ログおよび監視を有効化する

高可用性構成（Multi-AZ等）は初期フェーズでは必須とせず、
小規模運用を前提とする。

## 10. 仕様・設計の参照先
- 画面仕様：`05_screen_spec.md`
- API仕様：`04_api_spec.yaml`
- DB設計：`03_database.md`
- ログ設計：`06_log_design.md`
- 認証・認可：`07_security.md`
- デプロイ：`08_deploy.md`
- 設計判断ログ（ADR）：`/docs/adr/`

## 11. 用語集（任意）
- 近況：
- 投稿：
- 返信：
- （自分が混乱しそうな用語だけ）
